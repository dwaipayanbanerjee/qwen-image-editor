# FastAPI and web server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0

# Data validation
pydantic>=2.5.3
python-dotenv>=1.0.0

# Image processing (updated for Python 3.13 compatibility)
Pillow>=10.3.0

# PyTorch with MPS support (Mac Apple Silicon)
# Note: torch>=2.0.0 includes MPS backend for Apple Silicon
torch>=2.2.0
torchvision>=0.17.0

# Hugging Face and Diffusers
# Note: diffusers>=0.35.0 includes GGUF quantization support for Qwen models
transformers>=4.40.0
accelerate>=0.28.0
diffusers>=0.35.0

# Additional ML dependencies
safetensors>=0.4.2
sentencepiece>=0.2.0
huggingface-hub>=0.20.0
gguf>=0.10.0

# Utilities
requests>=2.31.0

# Replicate API client (for Seedream-4)
replicate>=0.25.0
