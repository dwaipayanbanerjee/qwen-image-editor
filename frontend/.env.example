# Backend API URL Configuration
# Choose one of the following deployment options:

# Option 1: Full-stack deployment on RunPod (same server)
# Use this when running both backend and frontend on the same RunPod instance
# VITE_API_URL=http://localhost:8000

# Option 2: Split deployment - Backend on RunPod, Frontend locally
# Use this when running frontend on your local machine and backend on RunPod
# Replace <pod-id> with your actual RunPod pod ID (shown in RunPod dashboard)
VITE_API_URL=https://<pod-id>-8000.proxy.runpod.net

# Example (Remote RunPod backend):
# VITE_API_URL=https://2ww93nrkflzjy2-8000.proxy.runpod.net
